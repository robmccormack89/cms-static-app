{# 

  Blog post cover with a featured image background

  This fits behind the sticky header

  If featured_img exists, applies featured_img as bg with uk-light & overlay classes

  If no featured_img, does a pink background with uk-dark styling, which changes with the dark/light button

  This cover will only be processed when the post doesn't have a "style" property with "type" & "color"

#}
<section id="MainSingleCover" class="uk-background-cover uk-flex uk-flex-center uk-flex-middle {{ (single.featured_img) ? 'uk-light' : '' }}" style="{{ (single.featured_img) ? 'background-image: url(' ~ single.featured_img.src|resize(1920, 600, 'center') ~ ');' : '' }}">
  <div class="{{ (single.featured_img) ? 'uk-section uk-overlay-primary' : 'uk-background-pink uk-section' }} uk-width-1-1">
    <div class="uk-container uk-container-medium">
      <div class="single-cover-details uk-width-1-1">
        
        {% include (single.taxonomies.categories or single.read_time) ? '_post_meta.twig' ignore missing  %}
        
        <h1 class="uk-heading-large uk-margin-remove-top">{{single.title}}.</h1>
        
        {% include (single.author or single.date_time) ? '_post_meta_sub.twig' ignore missing  %}
        
        {% include (single.featured_img.frame_src and (single.format == 'video' or single.format == 'audio')) ? '_post_featured_frame.twig' ignore missing  %}
        
      </div>
    </div>
  </div>
</section>